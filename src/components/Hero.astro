---
---
<section class="relative pt-32 pb-20 md:pt-40 overflow-hidden hero-section">
  <div class="container-custom relative z-10">
    <div class="grid md:grid-cols-2 gap-12 items-center">
      <div class="slide-up">
        <h1 class="mb-6 text-secondary-900 dark:text-white leading-tight">
          <span class="text-primary-600 dark:text-primary-400">AI-Powered</span> SEO Platform<br>
          for modern businesses.
        </h1>
        <p class="text-xl text-secondary-600 dark:text-secondary-300 mb-8 max-w-lg">
          Chadix is an intelligent AI SEO platform designed to boost your search rankings, drive organic traffic, and maximize your online visibility.
        </p>
        <div class="flex flex-wrap gap-4">
          <a href="/contact" class="btn-primary">
            Start Free Trial
          </a>
          <a href="#features" class="btn-outline flex items-center">
            See Features
          </a>
        </div>
      </div>
      
      <div class="relative fade-in">
        <!-- Optimized SVG Hero Image -->
        <div class="w-full max-w-lg mx-auto">
          <!-- Simplified and optimized SVG -->
          <svg viewBox="0 0 800 600" xmlns="http://www.w3.org/2000/svg" class="w-full h-auto drop-shadow-xl" width="800" height="600">
            <rect width="800" height="600" fill="none"/>
            <g>
              <!-- Dashboard Background -->
              <rect x="150" y="100" width="500" height="350" rx="20" fill="#f0f9ff" stroke="#0ea5e9" stroke-width="2"/>
              
              <!-- Dashboard Header -->
              <rect x="150" y="100" width="500" height="60" rx="20" fill="#0ea5e9"/>
              <text x="180" y="140" fill="white" font-family="Arial" font-size="24" font-weight="bold">Chadix AI SEO Dashboard</text>
              
              <!-- Graph Area -->
              <rect x="180" y="180" width="300" height="150" rx="10" fill="white" stroke="#e2e8f0" stroke-width="1"/>
              
              <!-- Graph Lines -->
              <polyline points="200,300 240,260 280,280 320,220 360,240 400,180 440,200" fill="none" stroke="#0ea5e9" stroke-width="3"/>
              <polyline points="200,320 240,300 280,310 320,270 360,280 400,250 440,240" fill="none" stroke="#14b8a6" stroke-width="3"/>
              
              <!-- Graph Labels -->
              <text x="200" y="350" fill="#64748b" font-family="Arial" font-size="12">Jan</text>
              <text x="280" y="350" fill="#64748b" font-family="Arial" font-size="12">Mar</text>
              <text x="360" y="350" fill="#64748b" font-family="Arial" font-size="12">May</text>
              <text x="440" y="350" fill="#64748b" font-family="Arial" font-size="12">Jul</text>
              
              <!-- Sidebar -->
              <rect x="500" y="180" width="120" height="220" rx="10" fill="white" stroke="#e2e8f0" stroke-width="1"/>
              
              <!-- Sidebar Items -->
              <rect x="520" y="200" width="80" height="20" rx="5" fill="#0ea5e9"/>
              <rect x="520" y="240" width="80" height="20" rx="5" fill="#f0f9ff"/>
              <rect x="520" y="280" width="80" height="20" rx="5" fill="#f0f9ff"/>
              <rect x="520" y="320" width="80" height="20" rx="5" fill="#f0f9ff"/>
              <rect x="520" y="360" width="80" height="20" rx="5" fill="#f0f9ff"/>
            </g>
          </svg>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Teal section at the bottom with Trusted By -->
  <div class="mt-10 w-full overflow-hidden -z-10">
    <div class="relative bg-teal-400 dark:bg-teal-500 py-12 md:py-16">
      <div class="container-custom">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          <div class="text-center md:text-left">
            <h3 class="text-xl md:text-2xl font-semibold text-white">Trusted by leading companies</h3>
            <p class="text-teal-50 mt-2">Join thousands of businesses already using Chadix AI</p>
          </div>
          <div class="flex flex-wrap justify-center md:justify-end items-center gap-8">
            <!-- Simplified Company Logos -->
            <div class="text-white font-bold text-xl">Company A</div>
            <div class="text-white font-bold text-xl">Company B</div>
            <div class="text-white font-bold text-xl">Company C</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Add keyframes for additional animations if needed */
  @keyframes float {
    0% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
    100% { transform: translateY(0px); }
  }
  
  /* Apply floating animation to the entire SVG container */
  .fade-in svg {
    animation: float 6s ease-in-out infinite;
  }
  
  /* Critical styles for LCP optimization */
  .hero-section h1 {
    font-size: 2.5rem;
    line-height: 1.2;
    font-weight: 700;
    margin-bottom: 1.5rem;
  }
  
  @media (min-width: 768px) {
    .hero-section h1 {
      font-size: 3.5rem;
    }
  }
  
  /* Optimize animations for performance */
  .slide-up {
    opacity: 1;
    transform: translateY(0);
    transition: opacity 0.5s ease, transform 0.5s ease;
  }
  
  /* Container styles */
  .container-custom {
    width: 100%;
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 1rem;
  }
  
  /* Button styles */
  .btn-primary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    font-weight: 500;
    background-color: #0ea5e9;
    color: white;
    text-decoration: none;
    transition: background-color 0.2s;
  }
  
  .btn-outline {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    font-weight: 500;
    border: 2px solid #e2e8f0;
    color: #1e293b;
    text-decoration: none;
    transition: background-color 0.2s;
  }
</style>
